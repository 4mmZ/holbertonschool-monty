#include "monty.h"

 char (*fcompare(char **opcode) {

    instruction_t fstruct[] = {
        {"push", }
        {"pall", }
        
        };

    if (opcode)
    {
        for(int function = 0; function < 3; function++) {
            if (strcmp(opcode == fstruct[function].opcode)) {
                return(fstruct[function].f)

            }
        }
    }
    
}